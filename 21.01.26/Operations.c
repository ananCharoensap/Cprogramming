#include <stdio.h>
#include <stdbool.h>
// import library

void main() {

    // =================== ตัวแปร ===================
    int num1, num2, sum;
    float result;
    bool total[10];

    // =================== end-ตัวแปร ===================


    // =================== input ===================
        // printf("Input num 1 : ");
        // scanf("%d", &num1);
        // printf("Input num 2 : ");
        // scanf("%d", &num2);

    // =================== end-input ===================


    // =================== การดำเนินการทางคณิตศาสตร์  ===================
        // result = num1 + num2;
        total[0] = 2 > 1;
        // shift + alt + key down;
        total[1] = 200 <= 201;
        total[2] = 2 == 2;
        total[3] = 2 != 2;
        total[4] = !((3 < 5) && (6 >= 7));
        total[5] = !((5 <= 20) || (6 > 9));
        total[6] = 2 != 2;

        sum = 5;
        sum *= 10;



    // =================== end-การดำเนินการทางคณิตศาสตร์  ===================


    // =================== Output  ===================
        // printf("Total 1 : %d\n", total[0]);
        // printf("Total 2 : %d\n", total[1]);
        // printf("Total 3 : %d\n", total[2]);
        // printf("Total 4 : %d\n", total[3]);
        // printf("Total 5 : %d\n", total[4]);
        // printf("Total 6 : %d\n", total[5]);
        // printf("Total 7 : %d\n", total[6]);
        printf("Total 7 : %d\n", sum);

    // =================== end-Output  ===================



}